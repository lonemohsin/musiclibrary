import React, { useEffect } from 'react'

const Album = ({ album }) => {
  // useEffect(() => {
  // }, [])
  return album.title
  // return <div className="">Hello{album.title}</div>
}

export default Album